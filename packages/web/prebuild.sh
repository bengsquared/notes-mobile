#!/bin/bash
# This script runs before the build to copy any needed external files

# Example: If you need the shared package later
# cp -r ../shared ./shared-temp

# For now, we don't need anything from outside packages/web
echo "Prebuild complete"