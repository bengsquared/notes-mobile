{
  "buildCommand": "cd packages/web && npm run build",
  "outputDirectory": "packages/web/.next",
  "installCommand": "npm install -g pnpm@8 && pnpm install --frozen-lockfile",
  "framework": "nextjs"
}