legacy-peer-deps=true
engine-strict=false
enable-pre-post-scripts=true

# Enable hoisting for better electron compatibility
shamefully-hoist=true

# Ensure electron binaries are downloaded
ELECTRON_SKIP_BINARY_DOWNLOAD=false

# Better compatibility with native modules
node-linker=hoisted